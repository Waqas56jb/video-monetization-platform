# MTONYO+ — final A–Z re-verification

Run: 2026-09-25, against production (client + API both on `X-Build: 277ecb4` at the start of the run).
Every row below comes from a command run in this pass. Nothing is carried over from an earlier report.
Raw outputs are in the scratch evidence folder referenced per row; the ones worth keeping are copied into
`scripts/e2e/evidence/final-audit-2026-09-25/`.

Statuses: **VERIFIED WORKING** · **FOUND BROKEN, FIXED** · **FOUND BROKEN, NOT YET FIXED** · **CANNOT VERIFY FROM HERE**

---

## Section A — Player & playback

Profiles: "desktop" = Chromium 1440×900, no throttling. "Pixel 7 throttled" = Pixel 7 emulation, CPU ×4,
150 ms / 1.6 Mbps. Each timed row is the median of 5 fresh browser contexts: open Explore, tap the card,
then measure tap→poster (the poster image has loaded and is on screen) and tap→playing (`currentTime` is past
0.2s and advancing inside the Cloudflare iframe).

| Item | Status | Evidence (this run) |
|---|---|---|
| A1 Poster on tap, fast first frame | VERIFIED WORKING (desktop) · honest number for throttled mobile below | Tap→poster, median: **0.71–0.75 s** desktop, **0.61–0.83 s** Pixel 7 throttled, on all 3 videos. Tap→playing, median, desktop: Live at Arusha **2.80 s**, Behind The Fame preview **2.68 s**, free+ads (first frame is the advert) **3.26 s**. Pixel 7 throttled: **7.83 s / 7.83 s / 8.11 s**. 30/30 runs reached playback. (Correction: the first row was meant to be "owned, signed in", but the probe wrote the session under the wrong storage key, so both paid rows measured the signed-out preview. It boots the same Cloudflare player, and the owned-film start is covered by the resume timings in A3 and L. The same key bug was in the committed `probe-video-startup.mjs` and was fixed in `ec6d91d`.) `A1-timing.txt`, `A1-timing-freeads-rerun.txt` |
| A2 Buying A never unlocks B | VERIFIED WORKING | `purchase-journey.mjs` with 2 brand-new accounts (`e2e+buy0292008633`, `e2e+buy0292087647`), Chromium desktop + Pixel 7. Before paying: `kind=preview`. After the sandbox payment, the bought video returns `kind=full`; the other paid title stays `kind=preview` and still shows Unlock. Checked again after logout→login, in the browser **and** with curl using the new account's token (`rpreplay…: kind=full canWatchFull=true`, `live-at-arusha…: kind=preview`, `behind-the-fame…: kind=preview`). Declined and cancelled payments leave the video locked. `A2-purchase-journey.txt` |
| A3 Resume from the exact preview stop, no extra button | VERIFIED WORKING | Preview halted at 19.9 s (cut-off 20 s). After payment the film was playing at **20.0 s**, with no second Play button. Server row `watch_progress.seconds = 20` for both new buyers, against `stopsAtSeconds = 20`: a difference of 0 s. |
| A4 Portrait not squashed | VERIFIED WORKING | `rpreplay…` (source 886×1920, w/h 0.461): the player frame is **330×718 (0.460)** on desktop and **308×669 (0.460)** on Pixel 7. `A4-portrait-*.png` |
| A5 Top progress bar clears | VERIFIED WORKING | 7 navigations (Home → Explore → watch → back → watch → /creator-capital → Home). `.top-progress` elements 5 s after each one: **0, 0, 0, 0, 0, 0, 0**. |
| A6 Preview cap + label = real cutoff | VERIFIED WORKING | `clampFreePreviewSeconds` called directly: 30 s → 10, 60 → 20, 83 → 27, 366 → 122, 653 → 217, 900 → 300, 1800 → 300, 7200 → 300 (never more than ⅓ or 5 min, whichever is shorter). DB: **0** videos in any state are stored above the cap. API: all 5 published paid videos return `stopsAtSeconds = freePreviewSeconds`. On-screen label equals the server cutoff: "3:37 free preview" = 217 s, "0:20" = 20 s, "0:30" = 30 s. |
| A7 Countdown only after real ad airtime | VERIFIED WORKING, **plus a real bug found & fixed** | 150 ms DOM trace, signed out. Desktop: first ad airtime 6385 ms, first "Skip in 5" 6858 ms. Pixel 7: 10435 / 11006 ms. **0** samples showed a countdown before airtime; **0** samples had nothing on screen ("Advert loading…/starting…" covers the wait). **Bug found:** the film was **autoplaying muted underneath the pre-roll**: film at 7.6 s by the time the advert ended, so the viewer missed the opening. Root cause: the iframe URL always requests autoplay, and the single `pause()` ran before Cloudflare acted on it. **Fixed in `aa974c1`**. After deploy, the film stays at `0.0 (paused)` for the whole advert and starts from 0.0 when it ends, both on direct load and from an Explore card. `A7-ads.txt`, `A1-trace-direct.txt` (before), `A7-film-held-after-*.txt` (after) |
| A8 Pre-roll / mid-roll / skip / target are admin settings that really apply | VERIFIED WORKING | Live `platform_settings`: `preroll_enabled=true, preroll_skip_after_secs=8, preroll_target_seconds=10, midroll_position_pct=70, midroll_after_secs=300, midroll_long_after_secs=1200, midroll_gap_secs=600, midroll_max_count=3`. Mid-roll scales with length: 366 s video → mid-roll at 256 s (70 %); 653 s → 457 s. **Live round trip through the real admin API:** set `midroll_position_pct` 70 → 50 and `preroll_target_seconds` 10 → 15; the ad schedule for Ugali & Samaki immediately changed from mid@457/target 10 to **mid@326/target 15**; both restored and re-read from the DB (70 / 10 / 8). The skip delay shows **5 s** because the running Vodacom campaign has its own per-campaign override, which takes precedence over the platform default by design (`adPayload`). |
| A9 Free+Ads after Paid Premiere auto-conversion | VERIFIED WORKING (simulated; no converted video exists on production today) | DB: 0 videos have converted yet (the earliest premiere ends 2026-09-27). Simulation using production code and the real row: Behind The Fame (653 s) with the premiere job's exact `SET` values applied **in memory, nothing written**, run through the live `adBreaksFor`, gives `pre_roll@0 skip=5 target=10 · mid_roll@457 · post_roll`, **identical** to the native Free+Ads Ugali & Samaki (653 s). Before conversion it correctly carries no ads. |
| A10 Overall Play → video starting | Honest number: **~2.7–3.3 s desktop, ~7.8–8.1 s on a throttled mid-range phone** | Same data as A1. The throttled-phone figure is dominated by Cloudflare's own player loading itself (measured 2026-09-21/23: ~5 s of the ~12 s before any media request, `scripts/e2e/evidence/video-startup-2026-09-23.txt`). Nothing in our code was found adding to it in this pass. |


## Section B — Sharing

| Item | Status | Evidence (this run) |
|---|---|---|
| B1 Mobile WhatsApp rich card | VERIFIED WORKING | Fresh URLs (`?v=<now>`) were requested with the WhatsApp Android (`… A`) and iOS (`… I`/`… i`) crawler UAs, including a full device-like header set (Accept, Accept-Encoding, Accept-Language, keep-alive). Every response was the crawler document with `og:title` = the real DB title, `og:description` = "WATCH FREE PREVIEW · \<creator\> · MTONYO+", absolute `og:image` and canonical `og:url`, all within the first 1 KB. The image returned 200 `image/jpeg`, 1200×630, 41–79 KB. `B2-matrix.txt` |
| B2 Desktop WhatsApp Web/Desktop card | VERIFIED WORKING for every request shape we can build · honest residue below | **Header matrix** (`desktop-share-matrix.mjs`, 4 never-tested videos × 9 shapes: 3 WhatsApp bot UAs, browser CORS preflight + GET from `Origin: https://web.whatsapp.com`, 2 Electron/Desktop UAs, 2 bare GETs, a real navigation): all **36/36 documents correct**. **Firefox's anomalous shape** (a real navigation reporting `Sec-Fetch-Mode: same-origin`) correctly gets the SPA shell, and the shell still carries the og tags. **CORS:** both preflights (page and image) answer `204` with `Allow-Origin: *` and `Allow-Methods: GET, HEAD, OPTIONS`. The GETs carried only `Allow-Origin`; browsers only read Allow-Methods on the preflight, but the acceptance asks for it on both, so it was **added in `d94510b`**. **Send side** (`whatsapp-desktop.mjs`): on Chromium and WebKit desktop the button is a real `<a href="whatsapp://send?text=<watch URL>">`, with no popup and no navigation away from the page. The "Opening WhatsApp… not installed? Open WhatsApp Web" fallback appears **12 ms (Chromium) / 26 ms (WebKit)** after the click. iPad keeps WhatsApp Web in a tab; iPhone gets the app. **Real clients, last 7 days** (`crawler_hits`, excluding this machine and test-shaped URLs): **57 fetches from 35 WhatsApp server IPs in the US** (`WhatsApp/2.23.20.0`, `… A`), 9 from Pakistan, 1 from Tanzania, **all 200 with the crawler document**. **Residue:** (1) the og:image's *first* fetch after a deploy took 1.6–2.8 s (serverless cold start plus edge cache miss); later uncached fetches take 0.75 s and cached ones 0.3–0.6 s. (2) Image fetches are not being written to `crawler_hits` (0 image rows for WhatsApp UAs, including this run's own), so image delivery to real clients can't be proven from telemetry, only from our own requests. (3) Whether the card is drawn before the user presses Send is WhatsApp's compose-time behaviour and is outside our control. |
| B3 IG / TikTok / FB / Copy / 60 s clip on **every** published video | VERIFIED WORKING | `share-matrix.mjs` on **all 8 published videos × 4 profiles** (Chromium desktop, Pixel 7, iPhone 14 WebKit, iPad Pro 11 WebKit) through the real share sheet: **288/288 checks passed**. Per video and profile: the card image decodes, "Card ready" appears, WhatsApp handoff, Instagram and TikTok have a real href, Facebook sharer, Copy link writes the real watch URL, the 60 s promo clip downloads (1.2–2.9 MB), and More Like This loads. `B3-share-matrix.txt` |
| B4 "More Like This" failures | VERIFIED WORKING | 32/32 loads inside the share matrix. Plus 24 direct requests (3 rounds × 8 videos): **24/24 HTTP 200, all with items**, mean 0.93 s. |
| B5 IG/TikTok handoff on a real iPhone/iPad | CANNOT VERIFY FROM HERE | The matrix proves the hrefs and the clip on WebKit iPhone/iPad profiles; the actual hand-off into the Instagram or TikTok app needs a device. On the device-only list. |
| B6 Shared link → exact video, muted autoplay | VERIFIED WORKING | Cold context, Instagram Android in-app-browser UA, fresh `?utm_source=ig&igsh=<now>` on 3 videos. All 3 landed on the **exact** `/watch/<slug>`; the first frame played **muted** at 4.4–8.9 s (throttle-free Pixel 7 emulation); the unmute control appeared. On the Free+Ads video the pre-roll ran first and the unmute control appeared once the film started. `B6-*.png` |

## Section C — Revenue split

| Item | Status | Evidence (this run) |
|---|---|---|
| C1 One global setting drives every surface | VERIFIED WORKING | Live value at the start: **`creator_split_percent = 70`**. Read at the same moment from **15 surfaces**, all 70. APIs: `/api/stats`, `/api/admin/settings`, `/api/admin/revenue` (`defaultSplitPercent`), creator `/api/earnings` (`splitPercent`). Rendered pages: Home hero stat, Home For Creators flow, viewer "Apply to become a Creator", creator dashboard Earnings ("You keep 70% of both"), Super Admin dashboard ("Current Global Split · 70% Creator / 30% Platform"), Creator Management split column, Revenue & Splits, Settings. **Payment calc:** today's two sandbox sales stored 500 → **350 creator / 150 platform, split 70**. **Platform earnings / revenue charts:** `/api/admin/revenue` totals (gross 325,325 · creators 223,106 · platform 102,219) equal the DB `earnings` sums exactly. **Withdrawal calc:** demo.asha API balance (available 15,350) = DB lifetime 15,350 − paid 0 − pending 0. **Payment reporting / analytics:** read the per-sale stored amounts; 0 purchases disagree with their own stored split. `C1-C2-split-rerun.txt`, `C2-creator-earnings.txt` |
| C2 Live flip 70 → 65 → 70 | VERIFIED WORKING | Through the real admin API. **Every one of the surfaces above showed 65 on the next load** (APIs, public Home, viewer, creator and Super Admin screens), then 70 after the restore. Restored value re-read from the DB: **70**. 70 is what is live now. |
| C3 History keeps its original split | VERIFIED WORKING | `purchases` / `earnings` hashed per stored split before and after the flips: 60 % (15 purchases, 56 earnings), 65 % (2 earnings, from the Sep 14 period), 70 % (49 purchases, 109 earnings) are **all byte-identical**. No historical row moved. |
| C4 No hard-coded split literal | **FOUND BROKEN, FIXED** | The existing audit (client + admin) had 0 "NN/NN" offenders, but **8 places painted a literal `70` while the live value loaded** (`?? 70` in Hero, For Creators, Testimonials, Become-a-Creator, Earnings, Creator Management ×1, Revenue ×2). A first-time visitor, or anyone whose request failed, saw 70 % even after an admin had changed the split. **Fixed in `d97cf00`**: these places show "—" or "your share" until the real value arrives. The audit gained a rule for split fallbacks; run against the old versions of those files it flags all 8, and against the new code 0. Checked live: with `/api/stats` held back, the hero reads "— of every sale is yours", then the real figure. |
| C5 Per-creator override | VERIFIED WORKING | No creator has an override today (all null). Live round trip on demo.asha via `POST /api/admin/creators/:id/split`: set **80** → their `/api/earnings` said 80 while the global stayed 70 → reset to **null** → back to 70. DB override is null again. |

## Section D — Viewer / Creator / Sub-admin / Super Admin separation

Two brand-new accounts were created for this: `e2e+aud-v294739873@mtonyo.test` (viewer only) and `e2e+aud-d294739873@mtonyo.test` (dual: signed up on Create, then added Watch). The dual account's Create profile was filled with a category, bio, payout number, payout method and an Instagram link through the real `PATCH /api/account`, so a leak would have something real to leak.

| Item | Status | Evidence (this run) |
|---|---|---|
| D1 Viewer-only never sees creator fields | VERIFIED WORKING | `POST /api/auth/login`, `GET /api/auth/me`, `GET /api/account`, `GET /api/account/analytics`: **0** payout / split / category / socials / followers / earnings fields; `creator: null`. Every Watch tab (library, purchases, activity, profile, settings) contains no creator tools. Sidebar: My Library, Explore, My Purchases, My Activity, Apply to become a Creator, Profile, Settings. |
| D2 Dual account on its WATCH side | VERIFIED WORKING | Same 4 endpoints on the Watch side: creator block reduced to **`{exists, displayName}`**, and **0** payout / split / category / socials / followers fields, although the Create profile has all of them filled. (`user.bio` does come back: it is the account-level bio on `profiles`, shared by both sides, and not a creator field.) Watch sidebar has no Upload, Earnings or My Videos. |
| D3 Create-side Analytics = creator metrics only | VERIFIED WORKING | Shows Total views, Paid unlocks, Viewers who paid (conversion), Published videos and the 30-day earnings chart. **None** of "In your library / Total spent / Watch time / Purchases". |
| D4 No creator wording on viewer Profile/Settings | **FOUND BROKEN, FIXED** | On a viewer-only account the Profile heading said "Your photo, name, bio, **category** and how people find you", and My Activity said "Views, **sales** and what is actually working". Both are Create-side wording. **Fixed in `2a0f682`**; live: "Your photo, your name and how to reach you." / "My Activity — What you have watched, bought and saved." The page bodies and email preferences had no "Tell people what you make", payout or approval language. |
| D5 Viewer↔Creator switch, clicked | VERIFIED WORKING | On the fresh (non-staff) dual account, "Open Viewer side" was actually clicked: side → `viewer`, URL → `?tab=library`, and the sidebar changed to the Watch tools. "Open Creator side" was then clicked: side → `creator`, `?tab=overview`, and the sidebar is identical to before. A viewer-only account is **not** offered the switch. |
| D6 Sub-admin scope | VERIFIED WORKING | `demo.moderator` (granted: review, moderation, videos). API: **200** on `/review`, `/videos`, `/reports`; **403** on `/users`, `/creators`, `/creator-applications`, `/payments`, `/withdrawals`, `/revenue`, `/capital`, `/audit`, `/ads`. All writes refused: `PATCH /settings`, split override, refund, withdrawal decision → **403** (split still 70). Admin site: the sidebar shows only Dashboard, Analytics, Notifications, Videos, Content Review, Moderation; `/users` and `/revenue` show "Administrators only"; `/capital` and `/settings` show "You cannot open this". (It can *read* `GET /settings`, by design; the 403 text on a settings write says "cannot view or change accounts", which is misleading wording but not a security issue.) |
| D7 Creator counts reconcile | VERIFIED WORKING | DB `role='creator'` **37** (all 37 have a creator profile) = Super Admin dashboard **37** = Creator Management rows **37** = User Management "Creators" **37**. |
| D8 Status counters reconcile | VERIFIED WORKING, **plus a latent bug fixed** | DB: 82 total / 81 active / 0 blocked / 1 suspended = dashboard = User Management cards. **Latent bug:** the User Management cards counted the rows of a list fetched with `limit: 100`, so from the 101st account they would have stopped at 100 and disagreed with the dashboard. **Fixed in `9f871ff`**: the cards now use the server counts. Live proof: with the list filtered to Creators (37 rows), the cards still read the platform totals 82 / 81 / 37 / 1. |

## Section E — Stability, performance, rate limiting

| Item | Status | Evidence (this run) |
|---|---|---|
| E1 No "Too many requests" in a real session | VERIFIED WORKING | `normal-session.mjs` on production, **22.1 min**: Home → Trending (hover) → Explore + filter → 3 videos watched ~170 s each → paid preview to its paywall → back/forward → Free+Ads advert watched through → Watch login → My Library (4 rows) + Purchases → an owned film → Create login + the whole creator dashboard → a **real sandbox payment** → Nyerere preview to paywall → **retry payment** (declined → Try again → cancelled) → another video → purchase refunded via the admin path. **171 API requests, peak 59 in any 60 s (limit 120), 0 × 429.** Server-side `rate_limit_hits` in that window: **0**; the last 429 anywhere on the platform was 2026-09-19, before that fix. |
| E2 The video that failed with ERR_FAILED | VERIFIED WORKING | 10 fresh loads each of Nyerere Day and Ugali & Samaki: **20/20 clean** — no failed request, no HTTP error, no error text (ready median 1.9 s / 2.5 s). |
| E3 Intermittent Trending/Home failures + auto-retry | VERIFIED WORKING | 20 fresh Home+Trending loads + 5 Trending (Explore sort) loads: **25/25 clean** (median 1.8 s). **Retry proven live:** with the first `/api/videos` request deliberately killed, the page made a second request by itself, rendered all 8 cards, and "No connection / Could not load" **never appeared**. |
| E4 Cold mobile Home load | Honest number: **LCP 2.30 s** (hero) · **video cards 2.96 s** | Pixel 7, CPU ×4, 150 ms / 1.6 Mbps, median of 5: DOMContentLoaded 1.82 s, **LCP 2.30 s** (range 2.0–3.4), 4 real video cards in the DOM **2.96 s**, first thumbnail decoded **3.06 s**. The Creator Capital section is still its own lazy chunk (`CreatorCapital-*.js`, 6.8 kB — confirmed again after this pass's changes). The hero is inside the 2–2.5 s range; the card grid is ~0.5 s over it. |
| E5 Mobile freeze / scroll | VERIFIED WORKING | Same throttled phone, 10 scroll flicks down and up on Home, Explore, a watch page and the dashboard: long tasks **1 in total (59 ms)**, none over 100 ms; layout shift **0.0000** on all four; no sideways overflow. |
| E6 First tap works (touch + mouse) | VERIFIED WORKING | `card-press.mjs`, every profile incl. **iPad Pro 11 + mouse**: poster, title, price row, creator name each open on **one** press; Save toggles on one press without opening the video. Plus the other primary controls on iPad+mouse (WebKit) and Pixel 7: **logo, Log in, Unlock, Share, Follow — each reacts on one tap (10/10)**. |
| E7 Login in one attempt, with autofill | VERIFIED WORKING | Viewer, creator and admin, on Chromium **and** WebKit, fields filled the way autofill fills them (value set, no input event): **6/6 signed in on one click, exactly 1 login request each**, 1.6–2.9 s. |
| E8 CI green, no deploy gap | VERIFIED WORKING, **plus the real cause of the "flakes" found and fixed** | Deployed `X-Build` = HEAD on API and client after every push in this pass. CI on `0f0a49c` (run #111): **48/48**. **Root cause of the recurring single-profile "flakes"** (#104, #108, #109 — a card not opening, a preview not halting, a blank watch page): each landed **the minute a push went live**. A chunk hash not in the current build answered **200 text/html** (the SPA fallback), so a page from the previous build got HTML for its Watch chunk and broke — the same thing a real visitor mid-session during a deploy would hit, and the service worker would even cache that HTML as the asset. Reproduced under a real deploy (`E8-deploy-skew.txt`: old-build page → error screen). **Fixed in `2ce128f`**: `/assets/*` misses are now a real **404** (checked live), the worker never caches HTML as an asset, and a failed lazy chunk reloads the page once. Proven live: first Watch-chunk request answering 404 → the page reloaded itself once and the watch page rendered, both from Explore and from a cold shared link. Journey 6 on Fast 3G re-run locally ×5: **5/5 pass**. **Final state:** CI #116 (`85d9903`) and #117 (`df704ff`, the final tree, no push overlapping it) **48/48 green**; API and client both serve `df704ff`. #113 was the entry-script case the second fix closes; #114 saw 9 Explore cards instead of 8 because this audit's own smoke run had its test video published for about a minute (see J3). |

## Section F — Creator Capital™

| Item | Status | Evidence (this run) |
|---|---|---|
| F1 One eligibility rule everywhere | VERIFIED WORKING | `platform_settings.capital_months_required = 6`; every surface (server capital routes, admin routes, `/api/stats`, homepage section, creator tab, info page, Super Admin Capital + Settings) reads that one value; **no** literal month count next to Capital/eligibility wording in UI code. Live: homepage card "4 of 6 months"; server refuses an ineligible creator "You need 4 more month(s)". |
| F2 Never two statuses at once | **FOUND BROKEN, FIXED** | Homepage example: exactly one status ("Building Eligibility"); demo.asha's live tab: exactly one ("Building Eligibility"). **Found:** every creator-tab state sat under a fixed "AIRPAY REVIEW" badge, so a declined request read "Request declined" **and** "AirPay review" at once (seen live, screenshot). **Fixed in `ff3b69c`** — the label names the partner ("AIRPAY MICROFINANCE"). **Open question for the client:** a declined request shows no way forward; whether/when a creator may ask again is AirPay's policy, so it was not invented here. |
| F3 Duplicate review requests prevented | VERIFIED WORKING | Live, real API: request with consent → accepted; **second request → 409 "You already have an open Creator Capital request"**; exactly **1** open row in the DB. Client: the under-review state renders no Request button. |
| F4 Admin wording + amount/terms required | VERIFIED WORKING | Buttons: **Record AirPay Approval / Record AirPay Decline / Publish AirPay Offer**. Live: approval with nothing → **400**; amount without terms → **400**; terms without amount → **400**; publishing an unapproved offer → **409**; the request unchanged by all four. The test request was then closed via "Record AirPay Decline" and afterwards removed (see J); the months rule, lowered to 1 for the test, is back at **6** (re-read from the DB). |
| F5 Consent before requesting | VERIFIED WORKING | Live: no `consent` → 400, `consent:false` → 400, both "You need to consent to MTONYO+ sharing your verified earnings…"; with consent, `consent_at` is recorded. The UI checkbox is required before the button enables. |
| F6 "Rights declared" wording | VERIFIED WORKING | 0 "Rights confirmed" in client/admin source; upload declaration: "I declare that I own this content or hold the necessary rights to it … every right, permission or licence needed". |
| F7 No absolute "never disappears" | **FOUND BROKEN, FIXED** | Public pages were already clean, but **Super Admin → Moderation carried a "PURCHASED CONTENT NEVER DISAPPEARS" badge**. **Fixed in `48c9d8f`** → "BUYERS KEEP ACCESS · EXCEPT LEGAL, RIGHTS OR SAFETY". 0 "never disappear" left in any source. |
| F8 "Creators earning" is real | VERIFIED WORKING | `creatorsEarning` = distinct creators with `creator_tzs > 0`, active, not demo (switch off) = **3**, matching the DB (Tiger, Waqas Naveed, Yasmin Chali). One of them only became "earning" through this audit's own sandbox purchases — reversed in J. |
| F9 Lender disclaimer verbatim everywhere | **FOUND BROKEN, FIXED** | Full sentence on the homepage section, the creator tab, Super Admin Capital, and once on the info page — whose **second** mention was a shortened tile ("AirPay Microfinance decides"). **Fixed in `7c36198`**. Live DOM count on `/creator-capital`: **2** complete sentences, desktop and mobile. |
| F10 Info page visual, not a Word page | **FOUND BROKEN (one section missing), FIXED** | Live, desktop + mobile: hero, 4-step flow, 5-item status legend, "Who decides" highlighted card, "If approved" offer card (with the AirPay row), CTA buttons; 0 `.legal-body`; no sideways scroll. **Missing:** the "Use your funding for" cards the acceptance lists — **added in `468cee2`** (the homepage's 4 cards, one shared list; the lazy homepage chunk stays split). Screenshots `F10-creator-capital-desktop.png` / `-mobile.png`. |
| F11 In the main navigation | VERIFIED WORKING | Desktop header link and the mobile menu's "Creator Capital" both go straight to `/creator-capital`. |

## Section G — Release model & admin controls

Run on a real published video (Behind The Fame, which the E2E fixture owns) through the real admin API; every column snapshotted first and restored.

| Item | Status | Evidence (this run) |
|---|---|---|
| G1 Three release models | VERIFIED WORKING | Enum = `permanent_paid, premiere_to_free, exclusive`. Live: **Exclusive** → buyer full, others preview; Exclusive + **free** (access change **keeps** it Exclusive) → everyone full; Exclusive + **unavailable** (unpublished) → anonymous 404. |
| G2 Original flag, admin-only, forces nothing | VERIFIED WORKING | The video's **own creator** setting Original/Exclusive → **400**, DB unchanged. Admin sets Original → release model, access, price and premiere end all unchanged (label only, per migration 036). |
| G3 Expiry job never converts Exclusive | VERIFIED WORKING | The job's own `isDue()` (evaluated from its source), window forced past: premiere_to_free → **true**, Exclusive → **false**. The job's own selection with the clock pushed 100 years ahead picks the Exclusive video **0** times. |
| G4 Buyers keep access through changes | VERIFIED WORKING | After Exclusive + ppv + **unpublished**, the fixture still gets **`kind=full`**; anonymous gets 404. |
| G restore | Restored (one side-effect corrected) | Switching the premiere to free and back made the DB trigger restart its paid window (it would have ended 2026-11-24 instead of 2026-10-04). No admin action can set a start date, so the original `premiere_started_at` was written back directly (one row, one column, checked transaction); every column now equals the snapshot. **Note for the client:** changing a premiere to free and back restarts its window — that is how the trigger is designed. |
| G5 Ad controls really change playback | VERIFIED WORKING | See A8: a live change of mid-roll position and pre-roll target moved the real schedule (457 → 326 s, 10 → 15 s) and back; the skip delay in effect is the campaign's own override (5 s). Reporting: `/api/admin/revenue` totals equal the DB `earnings` sums to the shilling. (Dashboard revenue rose TZS 25 during this run because this audit's probes watched the Vodacom advert to completion — see J.) |

## Section H — Financial accuracy

| Item | Status | Evidence (this run) |
|---|---|---|
| H1 "Paid Out" = completed withdrawals only | **FOUND BROKEN, FIXED** | Creator dashboard "Already paid out" = `withdrawals.status='paid'`; Super Admin Withdrawals "Paid Out" = **TZS 1,000** = the only completed withdrawal on the platform. **Found:** the homepage "Creators earning now" spotlight captioned each creator's **lifetime earnings** as "**paid out to them so far**" — e.g. Waqas Naveed TZS 46,620 while their completed withdrawals = **TZS 0** (the same for all three shown). **Fixed in `0f0a49c`** → "earned on MTONYO+ so far". |
| H2 No double counting with a pending withdrawal | VERIFIED WORKING | Live on demo.asha: 15,350 available → TZS 1,000 withdrawal pending → **pending 1,000, available 14,350** (subtracted once) → admin reject → 15,350. |
| H3 Only M-Pesa / Airtel Money | VERIFIED WORKING | Real payment sheet (`H3-payment-sheet.png`): "M-Pesa · Vodacom", "Airtel Money · Airtel", no card images; 0 Visa/Mastercard in client source (only a comment recording their removal). |

## Section I — Super Admin general

| Item | Status | Evidence (this run) |
|---|---|---|
| I1 Logo → public site, session kept | VERIFIED WORKING | Admin logo `href=<public site>` `target=_blank`: opened the public homepage in a new tab; `/dashboard` still signed in afterwards. |
| I2 No internal/migration text | **FOUND BROKEN, FIXED** | The Sep 21 fix (042) cleaned `creator_applications`, but **six creator bios still read "Creator account created before applications existed."** (Yasmin Chali among them — shown on her public creator page). Cause: approving a backfilled application copies its description into an empty bio. **Fixed in `12e8b57`**: migration **043** (applied to production) clears the two exact system strings from bios — **0 left** — and approval no longer copies either string. |
| I3 Demo accounts out of public stats | **FOUND BROKEN, FIXED** | Live `show_demo_content_in_stats = false`. "Creators earning" and the top-creators strip obeyed it, but **"Earned by creators" read TZS 223,124, of which real creators had earned 52,920** — the rest demo and smoke-test ledger. **Fixed in `221c8e3`**; live `/api/stats` now **paidToCreatorsTzs 52,920**, creators 18. "Published videos" (8) still counts the visible catalogue, 6 of which belong to demo creators — that is what a visitor can actually browse. |
| I4 Library badges not covered | VERIFIED WORKING | 13 access badges (Paid Premiere / Pay Once / Still Yours / In Library) at 1440 px, Pixel 7 and 375 px: no visible control (×, save pin, Follow) overlaps any of them — the only element "over" them is the invisible full-card link, by design. Screenshots `I4-library-*.png`. |

## Section J — Test / demo data

| Item | Status | Evidence (this run) |
|---|---|---|
| J1 What test data exists right now | Enumerated (below) | By account pattern: **5** seeded demo (`@mtonyo.demo`), **36** smoke (`creator.*/viewer.*@mtonyo.test`, all flagged `is_demo`), **9** `e2e+`/`probe+` test accounts (the documented E2E fixture, 8 created by this audit, 1 older `probe+`), **1** other `@mtonyo.test`. **16** smoke videos (unpublished, stuck in "processing", visible in Super Admin Videos/Review). **1** announcement titled "test" (in 27 inboxes). **58** orphaned logins in `auth.users` with no profile: 49 `e2e+…@mtonyo.test` (Aug 31–Sep 9) and 9 `@mtonyo.demo`, left behind by earlier cleanups. **1** dangling creator profile on a sub-admin account. No duplicate creator applications. Public catalogue: 8 published videos, **6 from demo creators**; also "222222222222" (TZS 22,200) by "Waqas Naveed <as@gmail.com>" and "Nyerere Day — Rehearsals (awaiting review)" by a demo creator, both public. |
| J2 Cleaned vs remaining | Listed | `production-reset.mjs` (dry run, admin-path only): **45 accounts** in scope, **10 old smoke purchases to refund (TZS 9,500)**, **16 videos to soft-delete**, the "test" announcement, the dangling creator profile; "need a human: 0". **Not applied**: it deletes accounts, which is irreversible, and last pass's attempt was held for an explicit go-ahead that has not been given. **Reversed in this pass (admin path only):** every purchase this audit made (2 from A2, 1 from E1 by its own script, 4 from L) was refunded through `POST /api/admin/payments/:id/refund`; both test withdrawals were rejected through the admin decision; the Creator Capital test request was declined through the admin path and then removed (id-targeted, as the previous pass did); both live settings flips were restored and re-read from the DB; the follow the E6 probe added was removed; Behind The Fame's premiere window was restored exactly. **Left by this audit:** 8 `e2e+` accounts and 2 new smoke accounts (all listed in the dry run), plus about TZS 25 of ad credit from probes that watched the Vodacom advert to the end. |
| J3 Smoke suite cleans up after itself | **FOUND PARTLY BROKEN, FIXED** | Ran it on production: 42/42, both of its accounts flagged `is_demo` immediately, purchase refunded, video unpublished, no net earnings. **But** every run left its video in Super Admin's Videos and Review lists, stuck in "processing"; that is how 16 piled up and how the admin Review thumbnail 404 happened. It also carried the **live admin password as a literal in a committed file**. **Fixed in `a62ca13`**: cleanup now also soft-deletes its own video, and the admin credentials come from the environment. Re-run: **43/43**, its video removed from the admin lists. The 2 accounts per run remain (there is no admin route to delete an account), flagged demo, so they are invisible to every public number. **Still true, by design:** a smoke run publishes its test video on the live site for about a minute, to buy it, before unpublishing it. That is visible to real visitors and to CI (it caused CI #114). It should not be run against production while real people are browsing. |
| J4 Nothing deleted without listing | Held to | Nothing belonging to the client or to an unknown account was touched. Accounts that look like manual tests but are not a known test pattern (`applytest.*@gmail.com` ×5, `abc@gmail.com`, `creator2@gmail.com`, `as@/hamza@/waqas@/waqas1122@gmail.com` "Waqas Naveed", `ahmad1122@gmail.com`) are **listed only**; deciding on them is the client's or operator's call. |

## Section K — Retention features

| Item | Status | Evidence (this run) |
|---|---|---|
| K1 Follow | VERIFIED WORKING | DB: the trigger `follows_sync_counter` maintains the count; **0 of 38** creators drift (stored total 9 = 9 follow rows). `follow-cli.mjs` **ALL PASS** (a double follow counts once, the count matches the graph, a blocked creator cannot be followed but an existing follower can leave, zero drift). `follow-ui.mjs` **ALL PASS** (the control is on the watch page **and** on cards). Section L: Follow from the watch page is stored; one tap toggles on iPad+mouse and Pixel 7 (E6). |
| K2 Continue Watching | VERIFIED WORKING | `progress-write-path.mjs` **ALL PASS**: played to ~42 s, then the tab was killed, and the server row reads **42 s** (within 3 s). The server row after purchase = the preview stop (A3). L: Continue Watching holds the film after browsing away, on Chromium and Pixel 7. |
| K3 My List / Save | VERIFIED WORKING | `library-cli.mjs`: saving twice = one row, listed with a poster. `card-press.mjs`: the Save pin toggles on one press on all 5 profiles and does not open the video. iPhone WebKit, real touch tap on the pin: label flips and the **server stores it**. |
| K4 Recently Watched + remove (hide) | VERIFIED WORKING | `library-cli.mjs`: removing a film hides it from Recently Watched **and** Continue Watching, `hidden_at` is set, **the position is kept**, reopening resumes (17 s) and clears `hidden_at`. |
| K5 My Library — four rows | VERIFIED WORKING | `library-cli.mjs`: `purchased`, `continueWatching`, `myList`, `recentlyWatched` all present with real content. `library-ui.mjs` **ALL PASS**. Screenshot `I4-library-desktop.png` shows all four rows on the E2E fixture. |
| K6 Creator link on every card, no conflict with the card | VERIFIED WORKING | `card-press.mjs`: one press on the creator's name → `/creator/<id>`; one press on poster/title/price → the video; on 5 profiles. `creator-page.mjs` **ALL PASS**. |
| K7 Creator profile lists videos with Watch + Share | VERIFIED WORKING | `creator-page.mjs` **ALL PASS**. L (all 3 profiles): the creator page lists their videos with Watch (20 links) and Share (10). |

## Section L — The full journey, in one go, on fresh accounts

`secL` evidence: `L-journey.txt` (Chromium desktop), `L-journey-2.txt` (Pixel 7, iPhone WebKit, creator, admin). Each viewer run is **one continuous browser session on a brand-new account**.

| Leg | Chromium desktop | Pixel 7 | iPhone 14 WebKit (no video decode) |
|---|---|---|---|
| Register → signed in | PASS | PASS | PASS |
| Home + Trending, Explore | PASS | PASS | PASS |
| Save to My List from a card | PASS | PASS | PASS *(the scripted press missed on WebKit because of a wrong selector in my probe; a real touch tap on the pin, checked separately, flipped it and the server stored it)* |
| Open from a card → exact video | PASS | PASS | PASS |
| Preview autoplays **muted** | PASS (t=0.6, muted) | PASS (t=0.5, muted) | not claimed (WebKit cannot decode video) |
| Preview **stops at the cut-off** | PASS 216.9 s / 217 s | PASS 216.9 s / 217 s | not claimed |
| Unlock → sandbox pay → sheet closes | PASS | PASS | PASS |
| Server says `kind=full` | PASS | PASS | PASS |
| **Continues from the stop point, no extra button** | PASS 217.0 s vs 216.9 s | PASS 217.0 s vs 216.9 s | not claimed |
| Follow from the watch page | PASS | PASS | PASS |
| Free + Ads: advert first, countdown when it airs | PASS ("Skip in 5") | PASS ("Skip in 5") | advert shown PASS; countdown not claimed |
| Browse away → Continue Watching | PASS | PASS | not claimed |
| Share → WhatsApp link is the exact watch URL, and the crawler gets a rich card (title + 200 image/jpeg) | PASS | PASS | PASS |
| Log out → log in in one attempt (autofill-style) | PASS | PASS | PASS |
| Library state survived (Purchased + My List) | PASS | PASS | PASS (API: purchased + myList both hold the film) |
| Cold shared link (Instagram in-app UA) → exact video, muted autoplay | PASS | PASS | lands on exact video PASS; playback not claimed |
| Creator's public page (Watch + Share) | PASS | PASS | PASS |
| **Creator (demo.asha)**: overview, earnings (Request withdrawal), Creator Capital status, Request AirPay Review present and locked until eligible | PASS | | |
| **Admin**: dashboard numbers = server (90 users / 39 creators at that moment), split live change → public stats 65 → restored 70, ad control live change (pre-roll target 12) → applied to a real video → restored 10, Creator Capital admin with disclaimer, logo → public site in a new tab, admin session kept | PASS | | |

The first Pixel 7 attempt stopped at the logout step, because my probe clicked a Log out button inside the closed mobile drawer. It was re-run from scratch on another fresh account after fixing the probe, and that run passed every leg. All 4 purchases made in L were refunded through the admin path.

---

## Everything fixed in this pass

| Commit | What |
|---|---|
| `aa974c1` | **Player:** the film was playing muted *underneath* the pre-roll, so viewers came out of the advert 7–8 s into the film. |
| `2ce128f`, `85d9903` | **Deploys:** a page open across a deploy broke on its next page (missing chunk served as HTML), and a load that landed mid-deploy could miss its own entry script. This was also the cause of the recurring single-profile CI "flakes". |
| `221c8e3` | **Public stats:** with demo content switched off, "Earned by creators" still counted demo/smoke money (223,124 → 52,920 real). |
| `0f0a49c` | **Homepage:** lifetime earnings were captioned "paid out to them so far"; the real paid-out figure for those creators is TZS 0. |
| `12e8b57` | **Creator bios:** internal backfill text "Creator account created before applications existed." was in six public bios; migration 043 applied, and approval can no longer copy it. |
| `d97cf00` | **Split:** 8 places painted a literal 70 % while the real split loaded; the audit now fails on that pattern. |
| `2a0f682` | **Viewer dashboard:** Watch-side headings said "category" and "sales". |
| `9f871ff` | **User Management:** counters counted a 100-row list, so they would have stopped at 100. |
| `48c9d8f` | **Super Admin Moderation:** "PURCHASED CONTENT NEVER DISAPPEARS" → exception stated. |
| `ff3b69c` | **Creator Capital tab:** "AirPay review" label beside "Request declined" read as two statuses. |
| `7c36198` | **Creator Capital page:** second lender disclaimer was shortened. |
| `468cee2` | **Creator Capital page:** "Use your funding for" section added. |
| `d94510b` | **Sharing:** GET responses now also carry `Access-Control-Allow-Methods`. |
| `a62ca13` | **Smoke suite:** live admin password removed from committed source; runs no longer leave a stuck video in Super Admin. |
| `ec6d91d` | **Test tooling:** the video-startup probe used the wrong session keys (it was measuring signed out). |

Tests after the last change: client **255/255**, server **209/209**, admin **13/13**; client `verify` (tests + hover audit + split audit + build + render smoke) green before every push.

## Device-only — still needs a real phone / Mac

1. **Safari/iOS video playback**: Playwright's WebKit cannot decode Cloudflare's stream (no Media Source Extensions), so muted autoplay, preview cut-off, resume and advert countdown on Safari/iPhone are proven only on Chromium here.
2. **An actual WhatsApp send**: whether WhatsApp (phone app, Desktop, Web) draws the card *before* the person presses Send is WhatsApp's own compose-time behaviour. Every request shape it can make is answered correctly (B1/B2).
3. **Instagram / TikTok hand-off on a real iPhone/iPad** (B5): the links and the downloaded clip are proven; the jump into the app is not.
4. **iOS Low Power Mode**: cannot be emulated.

## Open decisions for the client (not guessed at)

- **Creator Capital after a decline:** the tab shows "Request declined" with no way forward. The server would accept a new request. Should a declined creator be able to ask again, and after how long? (AirPay's policy.)
- **Premiere window restart:** if an admin switches a Paid Premiere to Free + Ads and back, its paid window starts again from that moment (DB trigger design).
- **Test data cleanup** (J2): the 45 test accounts, 10 old smoke purchases, 16 smoke videos, the "test" announcement and 58 orphaned logins are listed and ready to remove through the admin path. This needs an explicit go-ahead because account deletion cannot be undone.
- **The Super Admin password** for `admin@mtonyo.tz` was committed in source (now removed) and remains in git history and in `report.txt`. **It should be changed.**
- **Visible test/demo content in the public catalogue:** "222222222222" (TZS 22,200) and "Nyerere Day — Rehearsals (awaiting review)" are published and public.

## Summary for the client (plain language)

Everything was re-tested from scratch on the live site in this pass: over 60 checks across playback, payments, sharing, the revenue split, account separation, stability, Creator Capital, release models, money figures, Super Admin, retention features and a full end-to-end journey on brand-new accounts, on desktop and phone. Most of it was already working and is now proven again with fresh evidence: payments unlock only the video bought and resume exactly where the preview stopped, the split changes everywhere at once and history is untouched, a 22-minute real session produced zero "Too many requests", and sharing works on every published video across four device types. The audit also found and fixed real problems that had not been reported: the film playing silently underneath the pre-roll advert, pages breaking for anyone who had the site open during an update, demo money inflating the public "Earned by creators" figure, a homepage caption calling earnings "paid out", and old system text showing as six creators' bios. All of these are fixed and confirmed live. What this computer cannot prove is video playback in Safari on a real iPhone, the moment WhatsApp draws a card before you press Send, and the jump into the Instagram/TikTok apps; those need a quick check on your own devices. Before launch we also recommend changing the admin password and approving removal of the listed test data.

---

## Follow-up, same day — decisions carried out (operator's go-ahead)

| Action | Result |
|---|---|
| **Admin password rotated** (`admin@mtonyo.tz`) | Set to a new random password with the server's own `admin.js password` tool. It is not written to any tracked file. Fresh login test: **old password → 401**, **new password → 200 (role=admin)**. CI workflows never used it. |
| **Test videos unpublished** (admin path, reversible) | "222222222222" and "Nyerere Day — Rehearsals (awaiting review)" → `is_published=false`. Public catalogue now **6** videos. |
| **Test-data cleanup** (`production-reset.mjs --apply`) | **10 old smoke purchases refunded** (TZS 9,500) through the admin refund path; **16 smoke videos soft-deleted**; the **"test" announcement deleted** (27 inbox rows with it); the dangling creator profile on a sub-admin removed; **27 test accounts deleted**. The documented E2E fixture was kept. |
| **Orphaned logins** | Account deletion cascades into the database's own protections (a video with purchase history cannot be hard-deleted), so Supabase refused many auth deletions and left the logins behind. That is how the original 58 orphans came about. All **81** orphaned test logins (55 `e2e+`, 17 smoke, 9 demo; each had no profile and matched a test pattern) were deleted with Supabase's admin API: **81/81, 0 orphaned logins left on the platform**. |
| **What remains, on purpose** | **18 smoke creator accounts** could not be deleted: their videos carry purchase history, which the database protects from hard deletion (the audit trail for money). All 18 are flagged demo, have **0 live videos** and **0 active purchases**, and are excluded from every public number. |
| After | 63 profiles · 20 live videos (6 published) · public `/api/stats`: Earned by creators **TZS 52,220**, 16 creators, 3 earning. |

Still open for the client: **Creator Capital after a decline**: may a declined creator request again, and after how long (e.g. 60–90 days), or never?

---

## Milestone 2 close-out polish (client's final list, same week)

| Item | Status | Evidence |
|---|---|---|
| **One clean loading screen** (no black → spinner → white → "Advert loading/starting" states) | **FIXED** (`c48af5f`) | Before, sampled every ~250 ms on a throttled Pixel 7: poster → **~6 s of Cloudflare's black box and spinner** → video; on Free + Ads, a black stage, a dark advert frame and "Advert loading… / starting…" in turn. After, on the same probe: the player box shows **the thumbnail with one MTONYO+ indicator from ~1.2 s until the first real frame** of the advert or the film, then cuts straight to playback (paid preview 10.4 s, Free + Ads 11.8 s on the throttled phone; 4.3–4.6 s on desktop). It lifts early only when autoplay is refused, the player fails, the boot sequence says "Still not starting", or 20 s pass, so it can never hide a player that needs a tap. The white frame the client described did not reproduce in any engine here (Chromium, WebKit, the advert → film handover), so whether it is gone on a real iPhone needs the client's device. It cannot show through anyway: nothing under the loader is visible until playback. |
| Play → first frame | Checked, honest number | Each player (advert, film) loads **exactly once** per play: no remounts or reloads. SDK preload, preconnects and the Watch-page prefetch are in place, and the advert request already runs in parallel with playback. The remaining ~3–4 s on desktop and ~8–10 s on a throttled phone is Cloudflare's own player bootstrap (its script is fetched through a redirect inside the embed), outside our code. |
| **Revenue split → Creator 60% / MTONYO+ 40%** | **DONE** | Set through the real admin API (`creator_split_percent` 70 → **60**, re-read from the DB). Read live from **12 surfaces**, all 60: 4 APIs, Home hero, Home For Creators flow, the viewer "Become a Creator" tab, the creator Earnings tab ("You keep 60% of both"), Super Admin dashboard, Creator Management, Revenue & Splits, Settings. Ad revenue uses the same setting. A full source sweep (client, admin, server, API routes, HTML, SQL) finds **no** displayed 70/30 or 60/40 literal (2 hits, both code comments). **On a real sale**: a TZS 5,000 purchase credited the creator **+3,000 (60%)**, and the creator's own Earnings screen showed the new lifetime figure. Past sales keep the split they were made under. |
| **Gap above the first section after a nav click** | **FIXED** (`4ff79a5`, `b7b94af`, test fix `86db720`) | Before: **39 px desktop, 37 px tablet, 31 px mobile** of empty space between the header and each section's first line (Trending, How It Works, Features, For Creators, Stories). After: **15 px on every link, at 1440 / 834 / 390 px, from the homepage and from another page** (30/30 measurements). The page's own spacing when scrolling is unchanged. |
| **Full end-to-end, fresh accounts** | PASS | Chromium desktop and Pixel 7, each one continuous session on a new account: register → Home/Trending → a card opens the exact video → preview autoplays muted and **stops at 216.9 s of 217** → Unlock → sandbox payment → sheet closes → server `kind=full` → **continues from 217.0 s by itself, no extra button** → creator credited at 60% → Follow stored → Free + Ads plays the advert first with its countdown → Purchased, My List, Continue Watching → WhatsApp link + rich card → log out / one-attempt log in → library intact → cold shared link lands on the exact video, muted autoplay → creator public page. iPhone WebKit: every leg except video decode. Creator dashboard (overview, earnings, Capital) and Super Admin (dashboard numbers = server; a live split change reached the public stats and was restored to 60; a live ad-control change reached a real video and was restored) pass. Separately: Follow (`follow-cli`), My List / Recently Watched / hide-keeps-position (`library-cli`), and the resume position written on tab kill and on pause (`progress-write-path`, 4 profiles) all pass. Recently Watched lists a Free + Ads film once the film itself has been watched (checked: 14 s past its advert). |
| Checks that failed in this round, with the cause | Explained | "Explore renders the catalogue" expected 8 cards; there are 6 now that the two test videos are unpublished (correct). "Recently Watched lists both films" failed because the journey only sat through the Free + Ads advert; the film had not been watched. The first `progress-write-path` run read 1 s because another check was rewriting the same test account's positions at the same moment; the clean re-run passed on all 4 profiles. CI #122 failed on a test file I pushed with a broken regex; fixed in the next commit, and CI #123 on the final tree is green. |
| Cleanup after testing | Done | The 3 journey purchases were refunded through the admin path (the creator's lifetime earnings are back to their pre-test figure, TZS 145,998), the 3 test accounts were removed, and their leftover logins were deleted (0 orphaned logins). Split still **60** in the DB. |

Still needs the client's own device: Safari/iPhone video playback (including whether any white frame remains there), an actual WhatsApp send, the Instagram/TikTok app hand-off.
